{
  "name": "jira-data",
  "version": "1.0.0",
  "description": "Monorepo for Jira Data Extractor",
  "private": true,
  "scripts": {
    "build": "pnpm -r run build",
    "cli": "pnpm --filter @jira-data/cli dev"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4"
  }
}
